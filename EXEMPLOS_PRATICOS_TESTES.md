# ?? Exemplos Pr�ticos: Testes Automatizados

## Cen�rio 1: Desenvolvedor Trabalhando Localmente

```bash
# Inicia o projeto com watch mode
dotnet watch test

# Resultado: Sempre que voc� salva um arquivo C#, os testes rodam automaticamente
# Feedback imediato sobre quebras de c�digo!
```

**Fluxo:**
1. Voc� edita `EventoService.cs`
2. Salva o arquivo (`Ctrl + S`)
3. Testes rodam automaticamente ?
4. Voc� v� resultado no terminal em segundos

---

## Cen�rio 2: Antes de Fazer Commit

```bash
# Executar testes com cobertura
dotnet test /p:CollectCoverage=true /p:CoverletOutputFormat=lcov /p:CoverletOutput=./coverage/

# Se tudo passar ?
git add .
git commit -m "feat: adicionar valida��o de convidado"
git push origin main
```

---

## Cen�rio 3: CI/CD no GitHub (Autom�tico)

**Voc� faz:**
```bash
git push origin main
```

**GitHub faz automaticamente:**
1. Dispara workflow em `.github/workflows/testes.yml`
2. Cria VM Ubuntu
3. Instala .NET 8
4. Executa testes
5. Gera relat�rio de cobertura
6. Publica resultados

**Voc� v�:**
- ? ou ? no commit
- Badge no README
- Relat�rio de cobertura

---

## Cen�rio 4: Validar Antes de Merge no PR

**Algu�m abre um Pull Request**

GitHub Actions **automaticamente**:
1. Executa todos os testes
2. Verifica cobertura
3. Publica coment�rio com resultados
4. Bloqueia merge se testes falharem

```
? Tests passed (15/15)
? Coverage: 85%
? Ready to merge!
```

---

## Cen�rio 5: Windows PowerShell Script

```powershell
# Script completo com op��es
PS> .\testes.ps1 -Coverage -Verbose

# Output:
??????????????????????????????????????????????????????????????????
?           ?? Testes Automatizados - Eventos API               ?
??????????????????????????????????????????????????????????????????

? .NET: 8.0.24
?? Gerando cobertura de c�digo...
?? Modo verboso ativado

??  Executando: dotnet test /p:CollectCoverage=true ... --verbosity detailed

[xUnit.net 00:00:00.83] Discovering: Eventos.Tests
[xUnit.net 00:00:00.86] Discovered:  Eventos.Tests
[xUnit.net 00:00:00.87] Starting:    Eventos.Tests
...
[xUnit.net 00:00:01.78] Finished:    Eventos.Tests

??????????????????????????????????????????????????????????????????
? ? TESTES EXECUTADOS COM SUCESSO!                              ?
??????????????????????????????????????????????????????????????????

?? Relat�rio de cobertura gerado em: ./coverage/coverage.info
```

---

## Cen�rio 6: Linux/Mac Bash Script

```bash
$ bash testes.sh --coverage --verbose

??????????????????????????????????????????????????????????????????
?           ?? Testes Automatizados - Eventos API               ?
??????????????????????????????????????????????????????????????????

? .NET: 8.0.24
?? Gerando cobertura de c�digo...
?? Modo verboso ativado

??  Executando: dotnet test /p:CollectCoverage=true ... --verbosity detailed

Aprovado!  � Com falha:     0, Aprovado:    15, Ignorado:     0

??????????????????????????????????????????????????????????????????
? ? TESTES EXECUTADOS COM SUCESSO!                              ?
??????????????????????????????????????????????????????????????????

?? Relat�rio de cobertura gerado em: ./coverage/coverage.info
```

---

## Cen�rio 7: Visual Studio Test Explorer

**Abrir:**
1. `Ctrl + E, T` ? Abre Test Explorer
2. V� lista de todos os testes
3. Clica em um teste espec�fico
4. Resultado aparece em tempo real

```
? AdicionarConvidado_DeveRegistrarComSucesso_QuandoDadosValidos
? AdicionarConvidado_DeveRegistrarComAcompanhantes_QuandoAcompanhadoComNomesValidos
? AdicionarConvidado_DeveRetornarBadRequest_QuandoNomeNulo
? AdicionarConvidado_DeveRetornarBadRequest_QuandoNomeVazio
...
(15 total - 15 passed)
```

---

## Cen�rio 8: Testes de um Arquivo Espec�fico

```bash
# Executar apenas testes de EventoService
dotnet test --filter "FullyQualifiedName~EventoService"

# Output:
Resumo do teste: total: 15; falhou: 0; bem-sucedido: 15; ignorado: 0
```

---

## Cen�rio 9: Monitoramento Cont�nuo em Produ��o

**Configura��o:**
- GitHub Actions executa testes a cada push
- ReportGenerator gera HTML visual
- Codecov monitora cobertura ao longo do tempo

**Resultado:**
- Badge no README: `![Coverage](https://img.shields.io/codecov/c/github/seu-usuario/Eventos)`
- Hist�rico de cobertura
- Alertas se cobertura cair

---

## Cen�rio 10: Integra��o com SonarQube (Avan�ado)

```yaml
# Adicionar ao .github/workflows/testes.yml
- name: SonarCloud Scan
  uses: SonarSource/sonarcloud-github-action@master
  env:
    GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    SONARCLOUD_TOKEN: ${{ secrets.SONARCLOUD_TOKEN }}
```

**Resultado:**
- An�lise de qualidade completa
- Detec��o de code smells
- Sugest�es de melhoria
- Dashboard visual

---

## ?? Fluxo Recomendado

```
1. Desenvolvimento Local
   ?
   dotnet watch test (feedback cont�nuo)
   ?
   
2. Antes do Commit
   ?
   dotnet test /p:CollectCoverage=true (valida��o final)
   ?
   
3. Push para GitHub
   ?
   GitHub Actions roda automaticamente
   ?
   
4. Resultado
   ?
   ? Sucesso ? Deploy autom�tico
   ? Falha ? Bloqueia merge
```

---

## ?? Combina��es de Comandos �teis

```bash
# Desenvolvimento r�pido
dotnet watch test

# Valida��o pre-commit
dotnet test /p:CollectCoverage=true

# CI/CD no servidor
dotnet test --verbosity minimal --logger "trx;LogFileName=TestResults.trx"

# Testes espec�ficos
dotnet test --filter "Category=Unit"

# Sem compila��o (ap�s build recente)
dotnet test --no-build

# Timeout customizado
dotnet test --blame-hang-timeout 30000

# Formato de sa�da para CI
dotnet test --logger "console;verbosity=minimal" --logger "trx"
```

---

## ?? Interpretando Resultados

```
Resumo do teste: total: 15; falhou: 0; bem-sucedido: 15; ignorado: 0; dura��o: 3,6s

?? total: 15              ? N�mero total de testes
?? falhou: 0              ? ? Nenhum falhou
?? bem-sucedido: 15       ? ? Todos passaram
?? ignorado: 0            ? Nenhum foi pulado
?? dura��o: 3,6s          ? ? Velocidade �tima
```

**O que significa:**
- ? Se `falhou: 0` e `bem-sucedido: 15` ? Tudo OK! Deploy liberado
- ? Se `falhou > 0` ? Corre��o necess�ria antes do merge

---

**?? Voc� agora domina testes automatizados!**
